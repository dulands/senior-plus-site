---
interface Props {
  kicker?: string;
  title: string;
  description?: string;
  id?: string;
  centered?: boolean;
}

const { kicker, title, description, id, centered = true } = Astro.props;
---

<div class:list={['section-heading', centered && 'section-heading--center']}>
  {kicker && <span class="section-kicker">{kicker}</span>}
  <h2 id={id}>{title}</h2>
  {description && <p>{description}</p>}
</div>
